apiVersion: v1
kind: Service
metadata:
  name: kaddem-app-service
  namespace: default
spec:
  selector:
    app: kaddem-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8090
  type: ClusterIP # Use LoadBalancer or NodePort for external access