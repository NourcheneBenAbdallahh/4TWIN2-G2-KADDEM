pipeline {
    agent any

    tools { 
        jdk 'JAVA_HOME' 
        maven 'M2_HOME' 
    }

    environment {

    }
    
   
    stages {
        stage('GIT') {
            steps {
                git branch: 'hadil', url: 'https://github.com/Nouunouu/4TWIN2-G6-KADDEM.git'
            }
        }

       

        stage('Compile Stage') {
            steps {
                sh 'mvn clean install -DskipTests'
            }
        }
}
}
