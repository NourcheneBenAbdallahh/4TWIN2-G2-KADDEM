pipeline {
    agent {
        node {
            label 'slave_build'
        }
    }

    tools {
        maven 'M2_HOME' // Must be configured in Jenkins > Global Tool Configuration
    }

    options {
        timeout(time: 30, unit: 'MINUTES')
    }

    environment {
        EXAMPLE_VAR = 'dev'
        NEXUS_REPO_URL = "http://127.0.0.1:8081/repository/maven-releases/"
        MAVEN_SETTINGS = "C:\\Users\\jenkins\\.m2\\settings.xml" // Adjust this path for Windows
        DOCKER_IMAGE_NAME = "oussabd/abdellaouioussema-g2-4twin2:latest"
    }

    stages {

        stage('Checkout Code') {
            steps {
                script {
                    try {
                        checkout([
                            $class: 'GitSCM',
                            branches: [[name: 'AbdellaouiOussema-4TWIN2-G2']],
                            userRemoteConfigs: [[url: 'https://github.com/Nouunouu/4TWIN2-G6-KADDEM.git']]
                        ])
                    } catch (e) {
                        echo "Failed to checkout code: ${e.message}"
                        throw e
                    }
                }
            }
        }

        stage('Build and Test') {
            steps {
                script {
                    try {
                        bat 'cd kaddem\\kaddem && call mvn clean install'
                    } catch (e) {
                        echo "Build failed: ${e.message}"
                        throw e
                    }
                }
            }
        }

        stage('SonarQube') {
            steps {
                script {
                    try {
                        withCredentials([string(credentialsId: 'sonarqube-token', variable: 'SONAR_TOKEN')]) {
                            bat 'cd kaddem\\kaddem && call mvn sonar:sonar -Dsonar.token=%SONAR_TOKEN% -DskipTests'
                        }
                    } catch (e) {
                        echo "SonarQube analysis failed: ${e.message}"
                        throw e
                    }
                }
            }
        }

        stage('Deploy to Nexus') {
            steps {
                script {
                    try {
                        bat "cd kaddem\\kaddem && call mvn deploy --settings \"%MAVEN_SETTINGS%\" -DskipTests"
                    } catch (e) {
                        echo "Deployment to Nexus failed: ${e.message}"
                        throw e
                    }
                }
            }
        }

        stage('Docker Compose') {
            steps {
                script {
                    try {
                        bat '''
                            cd kaddem\\kaddem

                            echo Stopping and removing existing containers...
                            docker compose down --remove-orphans

                            echo Removing Prometheus and Grafana if they exist...
                            docker rm -f my-prometheus-container 2>nul || echo "Prometheus not running"
                            docker rm -f grafana 2>nul || echo "Grafana not running"

                            echo Rebuilding and starting containers...
                            docker compose build
                            docker compose up -d
                            docker compose ps
                        '''
                    } catch (e) {
                        echo "Docker Compose failed: ${e.message}"
                        throw e
                    }
                }
            }
        }

        stage('Push Docker Image to Docker Hub') {
            steps {
                script {
                    try {
                        withCredentials([usernamePassword(
                            credentialsId: 'dockerhub-credentials',
                            usernameVariable: 'DOCKER_USERNAME',
                            passwordVariable: 'DOCKER_PASSWORD'
                        )]) {
                            bat """
                                echo Logging into Docker Hub...
                                echo %DOCKER_PASSWORD% | docker login -u %DOCKER_USERNAME% --password-stdin

                                echo Tagging Docker image...
                                docker tag abdellaouioussema-g2-4twin2:latest %DOCKER_IMAGE_NAME%

                                echo Pushing Docker image...
                                docker push %DOCKER_IMAGE_NAME%
                            """
                        }
                    } catch (e) {
                        echo "Failed to push Docker image to Docker Hub: ${e.message}"
                        throw e
                    }
                }
            }
        }
    }

    post {
        success {
            echo "===== Pipeline executed successfully ====="
        }
        failure {
            echo "====== Pipeline execution failed ======"
        }
    }
}
